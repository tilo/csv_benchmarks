# frozen_string_literal: true

source "https://rubygems.org"

gem "csv"           # Ruby 3.4+ no longer bundles csv in default gems
gem "smarter_csv", "1.17.0"   # current installed version; multi-version comparison uses inline gem()
gem "rspec"
gem "rake"
gem "benchmark"
gem "benchmark-ips" # optional — main harness uses Benchmark.realtime

# frozen_string_literal: true

source "https://rubygems.org"

gem "csv"           # Ruby 3.4+ no longer bundles csv in default gems
gem "smarter_csv"   # current installed version; multi-version comparison uses inline gem()
gem "rspec"
gem "rake"
gem "benchmark-ips" # optional — main harness uses Benchmark.realtime
